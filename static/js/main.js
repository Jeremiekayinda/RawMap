/**
 * RawMap — Scripts principaux (ES6)
 * Point d'entrée global — la carte est gérée par map.js sur la page d'accueil.
 */

document.addEventListener('DOMContentLoaded', () => {
    if (!document.getElementById('rawmap-map')) {
        console.info('RawMap — Application initialisée.');
    }
});
